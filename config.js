/**
 * DMVCFramework Log Viewer Configuration
 * 
 * Configure the server URL here if the frontend is hosted separately from the backend.
 * If serverUrl is empty or null, relative paths will be used (default behavior).
 */
window.AppConfig = {
    // Example: 'http://localhost:8080'
    serverUrl: 'http://localhost:8080'
};
